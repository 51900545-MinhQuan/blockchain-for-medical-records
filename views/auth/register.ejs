<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Đăng ký</title>
    <link
      rel="stylesheet"
      href="/assets/vendors/iconfonts/mdi/css/materialdesignicons.css"
    />
    <link rel="stylesheet" href="/assets/vendors/css/vendor.addons.css" />
    <link rel="stylesheet" href="/assets/css/shared/style.css" />
    <link rel="stylesheet" href="/assets/css/demo_1/style.css" />
    <link rel="icon" href="/assets/images/logo_mini.png" />
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/css/toastr.min.css"
    />
  </head>

  <body>
    <div class="authentication-theme auth-style_1 pt-5">
      <div class="row">
        <div class="col-12 logo-section mb-4">
          <a href="#" class="logo">
            <img src="/assets/images/logo.png" alt="logo" />
          </a>
        </div>
      </div>
      <div class="row">
        <div class="col-lg-5 col-md-7 col-sm-9 col-11 mx-auto">
          <div class="grid">
            <div class="grid-body">
              <div class="row">
                <div
                  class="col-lg-8 col-md-10 col-sm-11 col-12 mx-auto form-wrapper"
                >
                  <form action="/auth/register" method="post">
                    <div class="form-group">
                      <label for="inputFullname">Họ và tên</label>
                      <input
                        type="text"
                        name="fullname"
                        class="form-control"
                        value="<%= oldData.fullname || '' %>"
                        placeholder="Họ và tên"
                      />
                    </div>
                    <div class="form-group">
                      <label for="inputEmail">Email</label>
                      <input
                        autocomplete="off"
                        type="email"
                        name="email"
                        class="form-control"
                        value="<%= oldData.email || '' %>"
                        placeholder="Email"
                      />
                    </div>
                    <div class="form-group">
                      <label for="inputPassword">Mật khẩu</label>
                      <input
                        autocomplete="new-password"
                        type="password"
                        class="form-control"
                        name="password"
                        value="<%= oldData.password || '' %>"
                        placeholder="Mật khẩu"
                      />
                      <small class="form-text text-info"
                        >Mật khẩu phải dài ít nhất 8 ký tự, bao gồm chữ hoa, chữ
                        thường, số và ký tự đặc biệt.</small
                      >
                    </div>
                    <div class="form-group">
                      <label for="reenterPassword">Nhập lại mật khẩu</label>
                      <input
                        type="password"
                        class="form-control"
                        name="confirmPassword"
                        value="<%= oldData.confirmPassword || '' %>"
                        placeholder="Nhập lại mật khẩu"
                      />
                    </div>
                    <div class="form-group">
                      <label for="inputBirthday">Ngày sinh</label>
                      <input
                        type="date"
                        name="birthday"
                        class="form-control"
                        value="<%= oldData.birthday || '' %>"
                        placeholder="Ngày sinh"
                      />
                    </div>
                    <div class="form-group">
                      <label for="inputPhone">Số điện thoại</label>
                      <input
                        type="number"
                        name="phone"
                        class="form-control"
                        value="<%= oldData.phone || '' %>"
                        placeholder="Số điện thoại"
                      />
                    </div>
                    <div class="form-group">
                      <label for="inputAddress">Địa chỉ hiện tại</label>
                      <input
                        type="text"
                        name="address"
                        class="form-control"
                        value="<%= oldData.address || '' %>"
                        placeholder="Địa chỉ hiện tại"
                      />
                    </div>
                    <div class="form-group">
                      <label for="inputIdentificationNumber">CMND/CCCD</label>
                      <input
                        type="text"
                        name="identificationNumber"
                        class="form-control"
                        value="<%= oldData.identificationNumber || '' %>"
                        placeholder="CMND/CCCD"
                      />
                    </div>
                    <div class="form-group">
                      <div class="form-check">
                        <input
                          class="form-check-input"
                          type="checkbox"
                          name="role"
                          value="2"
                          id="isDoctorCheckbox"
                        />
                        <label class="form-check-label" for="isDoctorCheckbox"
                          >Bác sĩ</label
                        >
                      </div>
                    </div>
                    <button
                      type="submit"
                      class="btn btn-sm btn-primary btn-block"
                    >
                      Đăng ký
                    </button>
                  </form>
                  <div class="signup-link">
                    <p>Bạn đã có tài khoản?</p>
                    <a
                      href="/auth/login"
                      style="font-weight: bold; color: orangered"
                      >Đăng nhập</a
                    >
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="/assets/vendors/js/core.js"></script>
    <script src="/assets/vendors/js/vendor.addons.js"></script>
    <script src="/assets/js/template.js"></script>
    <% if(errors.length> 0) {%>
    <script>
      toastr.warning("<%= errors %>");
    </script>
    <% }%>
  </body>
</html>
