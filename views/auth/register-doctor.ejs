<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Xác thực thông tin bác sĩ</title>
    <link
      rel="stylesheet"
      href="/assets/vendors/iconfonts/mdi/css/materialdesignicons.css"
    />
    <link rel="stylesheet" href="/assets/vendors/css/vendor.addons.css" />
    <link rel="stylesheet" href="/assets/css/shared/style.css" />
    <link rel="stylesheet" href="/assets/css/demo_1/style.css" />
    <link rel="icon" href="/assets/images/logo_mini.png" />
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/css/toastr.min.css"
    />
    <style>
      body {
        background: url("/assets/images/background.jpg") no-repeat center center fixed;
        background-size: cover;
      }
      .authentication-theme {
        background: transparent !important;
      }
      .grid {
        background: rgba(255, 255, 255, 0.4) !important;
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 15px !important;
      }
    </style>
  </head>

  <body>
    <div class="authentication-theme auth-style_1 pt-5">
      <div class="row">
        <div class="col-lg-5 col-md-7 col-sm-9 col-11 mx-auto">
          <div class="grid">
            <div class="grid-body">
              <div class="text-center mb-4">
                <img src="/assets/images/logo.png" alt="logo" style="max-width: 100%; height: auto; max-height: 140px;" />
              </div>
              <div class="row">
                <div
                  class="col-lg-8 col-md-10 col-sm-11 col-12 mx-auto form-wrapper"
                >
                  <h4 class="text-center mb-4">Xác thực thông tin bác sĩ</h4>
                  <form action="/auth/register-doctor" method="post">
                    <div class="form-group">
                      <label for="specialization">Chuyên khoa</label>
                      <input
                        type="text"
                        name="specialization"
                        class="form-control"
                        value="<%= oldData.specialization || '' %>"
                        placeholder="Ví dụ: Tim mạch, Răng-Hàm-Mặt"                  
                      />
                    </div>
                    <div class="form-group">
                      <label for="licenseNumber">Số giấy phép hành nghề</label>
                      <input
                        type="text"
                        name="licenseNumber"
                        class="form-control"
                        value="<%= oldData.licenseNumber || '' %>"
                        placeholder="Số giấy phép"
                      />
                      <small class="form-text text-info"
                        >Số giấy phép phải có định dạng: TEST-DOCTOR-(7 chữ số)</small
                      >
                    </div>

                    <button type="submit" class="btn btn-sm btn-primary btn-block">Hoàn tất đăng ký</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="/assets/vendors/js/core.js"></script>
    <script src="/assets/vendors/js/vendor.addons.js"></script>
    <script src="/assets/js/template.js"></script>
    <% if(errors.length > 0) {%>
    <script>
      toastr.warning("<%= errors %>");
    </script>
    <% }%>
  </body>
</html>
